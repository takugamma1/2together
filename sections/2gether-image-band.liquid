<div class="tg-image-band">
  <div class="tg-image-band-track" aria-hidden="false">
    {% for block in section.blocks %}
      <div class="tg-image-band-cell" {{ block.shopify_attributes }}>
        {% if block.settings.image != blank %}
          {{ block.settings.image | image_url: width: 800 | image_tag: loading: 'eager', fetchpriority: 'low', sizes: '40vw' }}
        {% else %}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {% endif %}
      </div>
    {% endfor %}
    {%- comment -%}Duplicate set for the seamless marquee loop. Eager-loaded
      because the duplicates start off-screen and are moved with a CSS
      transform — lazy-loading would leave them blank as they scroll in.{%- endcomment -%}
    {% for block in section.blocks %}
      <div class="tg-image-band-cell" aria-hidden="true">
        {% if block.settings.image != blank %}
          {{ block.settings.image | image_url: width: 800 | image_tag: loading: 'eager', fetchpriority: 'low', sizes: '40vw' }}
        {% else %}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "2Gether Image Band",
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "type": "image",
      "name": "Image",
      "limit": 8,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "2Gether Image Band",
      "blocks": [
        { "type": "image" },
        { "type": "image" },
        { "type": "image" }
      ]
    }
  ]
}
{% endschema %}
